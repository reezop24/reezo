# Reezo Landing

Simple static landing site with:
- `index.html` (user view)
- `owner.html` (owner link builder)
- animated CSS background
- dynamic extra buttons

## Local preview
Open `index.html` and `owner.html` in browser.

## Deploy (Vercel)
Import this repo with preset `Other`.
